package main

// Example plugin - to be implemented.

func main() {
	// Plugin entry point is via the exported Plugin symbol, not main().
	// This function exists only to satisfy the Go compiler for buildmode=plugin.
}
